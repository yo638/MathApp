@model MathApp.Models.BusinessModels.SearchTopics
@{
    ViewData["Title"] = "View";
    Layout = "~/Views/Shared/_LayoutIn.cshtml";
    var userFromSession = Context.Items["User"] as MathApp.Models.DbModels.User;
    var IdRoleUser = userFromSession?.IdRole;
}

<div style="margin-bottom:1.5rem">
    <table style="margin-top:5rem;wIdth:100%;">
        <tr>
            <td>
                @if (IdRoleUser.ToString() == "1") {
                    @if (Model.criteria.type == "lesson"){ <h1>Уроци</h1>}
                    else{<h1>Състезания</h1>}
                }
                else
                {
                    @if (Model.criteria.type == "lesson"){<h1>Моите уроци</h1>}
                    else{<h1>Моите състезания</h1>}
                }
                </td>
                    @if (Model.criteria.type == "lesson")
                    {
                        <td style="text-align:right"><a asp-action="Create" asp-route-id="1" target="_parent"><button class="btn btn-primary">Създай нов урок</button></a></td>
                    }
                    else
                    {
                        <td style="text-align:right"><a asp-action="Create" asp-route-id="2" asp- target="_parent"><button class="btn btn-primary">Създай ново състезание</button></a></td>
                    }
        </tr>
    </table>
    <hr />
</div>
    
<button onclick="myFunction()" class="btn btn-outline-primary" style="margin-bottom:1.5rem">Търсене</button>
<form method="post" Id="searchform" display="none">
    <table style="wIdth: 100%; margin-bottom: 1.5rem">
        <tr>
            <td colspan="5">
                <div class="form-floating mb-3">
                    <input type="text" name="criteria.name" class="form-control" Id="searchuslovie" value="@Model.criteria.name" placeholder="uslovie">
                    <label for="floatingInput">Търси в заглавието</label>
                </div>
            </td>
        </tr>
        <tr>
            <td colspan="5">
                <div class="form-floating mb-3">
                    <input type="text" name="criteria.description" class="form-control" Id="searchreshenie" value="@Model.criteria.description" placeholder="reshenie">
                    <label for="floatingInput">Търси в описанието</label>
                </div>
            </td>
        </tr>
        <tr>
            <td colspan="5">
                <div class="form-floating mb-3">
                    <input type="text" name="criteria.anywhere" class="form-control" Id="searchanywhere" value="@Model.criteria.anywhere" placeholder="anywhere">
                    <label for="floatingInput">Търси навсякъде</label>
                </div>
            </td>
        </tr>
        <tr>
            <td>
                <div class="form-label mt-4">Период на създаване на темата</div>
            </td>
            <td>
                <div class="form-label mt-4 me-3" style="text-align:right">От</div>
            </td>
            <td>
                <input type="date" class="form-control" name="criteria.fromDate" value="@Model.criteria.fromDate" Id="criteria.fromDate" />
            </td>
            <td>
                <div class="form-label mt-4 me-3" style="text-align:right">До</div>
            </td>
            <td>
                <input type="date" class="form-control" name="criteria.toDate" value="@Model.criteria.toDate" Id="criteria.toDate" />
            </td>
        </tr>
        <tr>
            <td>
                <div class="form-label mt-4">Сложност:</div>
            </td>
            <td>
                <div class="form-label mt-4 me-3" style="text-align:right">Клас</div>
            </td>
            <td style="wIdth: 30%">
                <div class="form-group">
                    <input type="hIdden" name="Id" value="@Model.criteria.category.Id" />
                    <select name="criteria.category.Grade" value="@Model.criteria.category.Grade" class="form-select" Id="criteria.category.Grade">
                        <option value="0">Всички</option>
                        <option value="1">1 клас</option>
                        <option value="2">2 клас</option>
                        <option value="3">3 клас</option>
                        <option value="4">4 клас</option>
                        <option value="5">5 клас</option>
                        <option value="6">6 клас</option>
                        <option value="7">7 клас</option>
                        <option value="8">8 клас</option>
                        <option value="9">9 клас</option>
                        <option value="10">10 клас</option>
                        <option value="11">11 клас</option>
                        <option value="12">12 клас</option>
                    </select>
                    <script>document.getElementById("criteria.category.Grade").selectedIndex = @(Model.criteria.category.Grade);</script>
                </div>
            </td>
            <td>
                <div class="form-label mt-4 me-3" style="text-align:right">Ниво</div>
            </td>
            <td style="wIdth: 30%">
                <div class="form-group">
                    <select name="criteria.category.Difficulty" value="@Model.criteria.category.Difficulty" class="form-select" Id="criteria.category.Difficulty">
                        <option value="X">Всички</option>
                        <option value="A">А (задачи за всички ученици)</option>
                        <option value="B">В (задачи за отлична подготовка)</option>
                        <option value="C">С (задачи за състезания)</option>
                    </select>
                    <script>
                             if ("@Model.criteria.category.Difficulty" == "A") document.getElementById("criteria.category.Difficulty").selectedIndex = 1;
                             else if ("@Model.criteria.category.Difficulty" == "B") document.getElementById("criteria.category.Difficulty").selectedIndex = 2;
                             else if ("@Model.criteria.category.Difficulty" == "C") document.getElementById("criteria.category.Difficulty").selectedIndex = 3;
                             else document.getElementById("criteria.category.Difficulty").selectedIndex = 0;
                    </script>
                </div>
            </td>
        </tr>

        <tr>
            <td colspan="5">
                <div class="form-floating mb-3">
                    <input type="submit" value="Търси" class="btn btn-primary" formaction="/Topics/Search/" style="wIdth: 100%; margin-top: 1.5rem">
                </div>
            </td>
        </tr>
    </table>
</form>

<div class="list-group">

    @if (Model != null)
    {
    @for (var i = 0; i < Model.topics.Count(); i = i + 1)
    {
        <input type="hidden" name="IdMathProblem" value="@Model.topics.ElementAt(i).Id" />
        <input type="hidden" name="IdMathProblem" value="@Model.topics.ElementAt(i).Name" />
        <input type="hidden" name="IdMathProblem" value="@Model.topics.ElementAt(i).UpdatedTimeAgo" />
        <a asp-action="Edit" asp-route-id="@Model.topics.ElementAt(i).Id" class="@(i%2==0 ? "list-group-item list-group-item-action flex-column align-items-start active" : "list-group-item list-group-item-action flex-column align-items-start")">
            <div class="d-flex w-100 justify-content-between">
                    @if (Model.criteria.type == "lesson"){<h5 class="mb-1">Урок @(i + 1)</h5>}
                    else{<h5 class="mb-1">Състезание @(i + 1)</h5>}
                <small>@Model.topics.ElementAt(i).UpdatedTimeAgo</small>
            </div>
            <p class="mb-1">@Model.topics.ElementAt(i).Name</p>
            <small>@Model.topics.ElementAt(i).Description</small>
        </a>
        }
    }

</div>

<script>
    document.getElementById("searchform").style.display = "none";
    function myFunction() {
        var x = document.getElementById("searchform");
        if (x.style.display === "none") {
            x.style.display = "block";
        } else {
            x.style.display = "none";
        }
    }
</script>


@if (ViewBag.Message != null)
{
<script>
        toastr["@ViewBag.MessageType"]("@ViewBag.Message");
        ViewData.Clear();
</script>
}