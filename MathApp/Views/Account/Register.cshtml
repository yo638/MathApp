@model MathApp.Models.DbModels.User

@{
    ViewData["Title"] = "Register";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/site.js"></script>


<h1 style="margin-top:2rem">Регистрация</h1>

<div class="form-group row">
    <hr />
    <form name="RegisterForm" method="post" action="Register" onsubmit="return validateUserRegistration()">
    <table width="65%" align="center">
        <tr>
            <td>
                <label class="form-label mt-4">Потребителско име:</label>
                <input type="text" name="Username" class="form-control" id="Username" placeholder = "Въведете потребителско име">
                <div id="usernameError" style="color:red"></div>
            </td>
        </tr>
        <tr>
            <td>
                <label class="form-label mt-4">Име:</label>
                <input type="text" name="Name" class="form-control" id="Name" placeholder = "Въведете вашето име">
                <div id="nameError" style="color:red"></div>
            </td>
        </tr>
        <tr>
            <td>
                <label class="form-label mt-4">Имейл:</label>
                <input type="text" name="Email" class="form-control" id="Email" placeholder = "Въведете имейл">
                <div id="emailError" style="color:red"></div>
                <input type="hidden" name="EmailConfirmation" value=0>
            </td>
        </tr>
        <tr>
            <td>
                <label class="form-label mt-4">Парола:</label>
                <input type="password" name="Password" class="form-control" id="Password" placeholder = "Въведете парола">
                <div id="passwordError" style="color:red"></div>
            </td>
        </tr>
        <tr>
            <td>
                <label class="form-label mt-4">Повторете паролата:</label>
                <input type="password" class="form-control" id="RepeatPassword"  placeholder = "Повторете паролата">
                <div id="repeatPasswordError" style="color:red"></div>
            </td>
        </tr>
        <tr>
            <td>
                <input type="submit" id="submitbtn" class="btn btn-primary" value="Регистрация" style="margin-top:2rem"/>          
            </td>
        </tr>
    </table>
    </form>
</div>

@if (ViewBag.Message != null)
{
    <script>
        toastr["@ViewBag.MessageType"]("@ViewBag.Message");
        ViewData.Clear();
    </script>
}